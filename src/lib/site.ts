/* Domínio oficial da Flauzino — fonte única usada por metadataBase
   (layout.tsx), robots.ts, sitemap.ts e o JSON-LD da home, para nunca
   divergir entre arquivos. */
export const SITE_URL = "https://www.flauzinosistemas.com.br";
